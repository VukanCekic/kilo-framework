@01_architect

Goal:
Initialize the Memory Bank for this project by analyzing the codebase at a high level.

IMPORTANT CONSTRAINTS:

- This is a LARGE repo → do NOT scan every file
- Focus only on:
  - package.json
  - tsconfig files
  - root config files
  - folder structure (1–2 levels deep)
  - key entry points (src/, services/, common/, etc.)
- Do NOT go deep into implementation details
- We are NOT adding new features → only identify cleanup, bugs, and improvements

---

Tasks:

1. Analyze the project structure and identify:
   - tech stack (languages, frameworks, infra)
   - main modules/services
   - key architectural patterns (if visible)

2. Identify potential issues at a high level:
   - inconsistent structure
   - possible tech debt areas
   - testing gaps (jest/playwright present?)
   - duplication risks
   - config problems

3. Create content for:

### project-state.md

Include:

- tech stack
- main modules/services
- current architecture summary
- known tech debt (high-level)
- current focus: "stabilization / cleanup / bug fixing"

### tasks.md

Create:

- 3–5 HIGH IMPACT tasks only
- MUST be:
  - cleanup
  - bug fixes
  - refactors
- NO new features

Prioritize:

- stability issues
- test reliability
- structure problems
- maintainability improvements

---

Output format:

Return ONLY:

1. project-state.md content
2. tasks.md content

Do NOT modify files yet — just generate content.
